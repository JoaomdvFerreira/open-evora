{
  "name": "open-evora",
  "private": true,
  "version": "0.0.0",
  "description": "Root developer commands and typed tools/research/ tooling. The Research Explorer app (apps/research-explorer/) owns its own separate dependencies.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "explorer": "npm run build-data --prefix apps/research-explorer && npm run dev --prefix apps/research-explorer",
    "explorer:build": "npm run build-data --prefix apps/research-explorer && npm run typecheck --prefix apps/research-explorer && npm run test --prefix apps/research-explorer && npm run build --prefix apps/research-explorer",
    "research:validate": "node tools/research/validation/cli.ts",
    "research:analyze": "node tools/research/analysis/cli.ts --all",
    "research:readiness": "node tools/research/readiness/cli.ts --all",
    "research:typecheck": "tsc --noEmit -p tools/research/tsconfig.json",
    "research:test": "node --test --experimental-strip-types tools/research/**/*.test.ts apps/research-explorer/benchmark/generator-contract.test.js",
    "research:check": "npm run research:typecheck && npm run research:test && npm run research:validate"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "yaml": "^2.9.0"
  }
}
