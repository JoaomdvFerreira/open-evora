{
  "prefix": "PRB-",
  "directory": "problems",
  "idField": "problem_id",
  "sourceModel": "docs/datamodel.md",
  "notes": "domain accepts either a single string or a list of domain codes; multi-domain problems are recorded as a list, consistent with the existing evidence.domain list convention. No enum is enforced on domain values. decision_basis is an OPTIONAL structure — see docs/investigationstrategy.md §6 (Decision basis and Eligibility). It is never required and no existing canonical PRB-* is backfilled with it. contract_version is a free-form string, not enum-checked, so future decision_basis contract revisions do not require a schema/validator change merely to be recorded. decision_basis.scope.bounded is an explicit human-authored boolean, never inferred from scope.geography/population/temporal text; booleanFields enforces it is true|false when present, not merely truthy. decision_basis.contradiction_search.performed and decision_basis.overlap_check.performed are likewise explicit human-authored booleans enforced by booleanFields.",
  "requiredFields": [
    "problem_id",
    "title",
    "domain",
    "geography",
    "geography.level",
    "affected_populations",
    "problem_statement",
    "evidence",
    "evidence_status",
    "validation_status",
    "digital_tractability",
    "existing_solutions",
    "status"
  ],
  "enums": {
    "evidence_status": ["discovered", "corroborated"],
    "validation_status": ["unvalidated", "partially_validated", "validated"],
    "digital_tractability": ["not_assessed", "low", "medium", "high"],
    "existing_solutions": ["not_assessed", "assessed"],
    "status": ["OPEN", "REJECTED", "DUPLICATE", "NON_DIGITAL", "ALREADY_SOLVED", "INSUFFICIENT_EVIDENCE"]
  },
  "booleanFields": [
    "decision_basis.scope.bounded",
    "decision_basis.contradiction_search.performed",
    "decision_basis.overlap_check.performed"
  ],
  "references": [
    {
      "field": "evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.manifestation.evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.consequence.evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.currentness.evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.contradiction_search.evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.supporting_evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.boundary_evidence",
      "isList": true,
      "targetPrefix": "EVD-",
      "targetDirectory": "evidence",
      "required": false
    },
    {
      "field": "decision_basis.overlap_check.related_problems",
      "isList": true,
      "targetPrefix": "PRB-",
      "targetDirectory": "problems",
      "required": false
    }
  ]
}
